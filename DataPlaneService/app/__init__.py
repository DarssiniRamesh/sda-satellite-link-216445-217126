"""
DataPlaneService FastAPI application package.

This package exposes the FastAPI application instance from app.main.
The ASGI import path can be either:
- "app.main:app" (preferred)
- "main:app" via the root-level shim main.py
"""
